# ScubaMob Task Tracker

Claude should update this file only when explicitly asked or when completing a roadmap task that clearly changes status.

## Active Work

- [ ] Triage and fix remaining CODE_REVIEW.md findings on `main`. All of §2 (the cross-app findings) is now closed; remaining work is the per-app clusters in §3 (accounts deep-dive) and §4 (dive domain: galleries/logbooks/diveshops/divegroups non-functional end-to-end), plus §5 (throttling/cookie hardening) and the timeout sweep.

## Recently Completed

- 2026-08-06: Closed `CODE_REVIEW.md` §2.6 (no authorization anywhere in `scuba/equipment/views.py`). Every view (`index`, `edit`, `delete_equipment`, `archive2`, `archive3`, `practice_requirements`, `practice_require_edit`, `delete_requirement`) now has `@login_required`, and every query/mutation is scoped to the logged-in user: `Equipment` lookups filter on `user=request.user` (via `get_object_or_404(..., user=request.user)` for single-object fetches, `.filter(user=request.user)` for lists); `EquipmentMaintenance` — which has no direct `user` field, only a FK to `Equipment` — is scoped via `equipment__user=request.user`. `practice_requirements`/`practice_require_edit`/`delete_requirement` additionally restrict `EquipmentMaintenanceForm`'s `equipment` field queryset to the caller's own equipment, closing a related gap the review's text didn't spell out as a separate bullet: without that, an authenticated user could still attach a maintenance record to *someone else's* equipment by ID even with `@login_required` in place, since `ModelChoiceField` defaults to `Equipment.objects.all()`. Along the way, found and fixed two more bugs that would have kept every affected view returning a 500 regardless of the authorization fix, discovered because they sit directly in the lines being rewritten: (1) `index`/`archive2`/`archive3` called `template.render(Context({...}))` — the pre-Django-1.10 API; this Django version's `Template.render()` requires a plain dict, so every call to these three views has been raising `TypeError: context must be a dict rather than Context` regardless of who's calling — fixed by passing a dict and `request` directly. (2) `archive2`/`archive3`/`practice_requirements`/`practice_require_edit`/`delete_requirement` referenced their templates without the `equipment/` prefix (`"archive2.html"`, `'add_requirement.html'`) even though the actual files live at `templates/equipment/archive2.html` etc. — every one of those five views was raising `TemplateDoesNotExist` unconditionally; fixed to reference the real paths. Verified via 11 new tests in `scuba/equipment/tests/test_views_authorization.py` that exercise every URL end-to-end (not just unit-test the view functions), which is what surfaced both bugs.
- 2026-08-06: Closed `CODE_REVIEW.md` §2.5 (dead IP/country signup-blocking code) by removing it, per explicit instruction, rather than reconnecting it. Removed `SignupForm.__init__(self, ip_address, ...)` and `set_ip_address()` (`scuba/accounts/forms/signup.py`) — they only ever stored `ip_address` on the instance; nothing in `clean()` or any `clean_<field>` ever read it back to actually block anything. Removed the matching call sites in `scuba/accounts/views/signup.py`: `SignupView.get_form_kwargs()` (which became a no-op override once the `ip_address` line was gone, so removed the whole method) and `ValidateEmail.post()`'s `form.set_ip_address(...)` call (that view's `self.get_form()` is already undefined/broken independent of this — pre-existing, separate bug, left alone as out of scope). Also removed the unused `BlockedCountry`/`InvalidSignup`/`InvalidIPAddress` imports, and a second, equally dead block in `SignupForm.save()` (`if hasattr(self, 'iso_country'): user.add_signup_country(...)` — nothing ever sets `self.iso_country` on a `SignupForm` instance either, confirmed by grep). Deliberately left the `BlockedCountry`/`InvalidSignup` models, their admin registrations, and their migration untouched — removing DB-backed models is a schema change with more blast radius than "delete the dead form-level wiring," and CLAUDE.md's rule against schema changes that discard data argues for leaving that as a separate, explicit decision if ever wanted. Deleted `test_blocked_ip_address` (`scuba/accounts/tests/test_account_forms_signup.py`) — the test exercised exactly the removed, always-broken code path, and was the project's single longest-standing known-failing test (flagged in every session this week). Updated the other 6 tests in that file to stop passing the now-removed `ip_address=` kwarg. `test_blocked_ip_address` no longer exists to fail — the project's full test suite has no known-failing tests left outside the pre-existing `view_profile` DB-view issue. (broken image/S3 upload pipeline). Fixed all five cited breakages in the shared upload infrastructure: `scuba/libs/imageuploader.py` — `Image.ANTIALIAS` (removed in Pillow ≥10) replaced with `Image.LANCZOS` (the same value under its non-deprecated name), and `S3.upload_raw_data(AWS_S3_BUCKET, filename, image_file, **header)`'s swapped arguments fixed to `S3.upload_raw_data(filename, image_file, bucket=AWS_S3_BUCKET, **header)` matching the real signature; `scuba/libs/models/awsmodel.py` — added the missing `S3`/`AWS_S3_BUCKET` imports (`delete()` and `upload_file()` both referenced undefined names), and `upload_file()` now calls `S3(...).upload_file(...)`, a method that actually exists, instead of the nonexistent `upload_from_filename`; `scuba/libs/fileutils.py` — `S3.upload_file_content(...)` (doesn't exist) replaced with the real `S3.upload_raw_data(...)`. Verified `content.Image` (a live `AWSModel` subclass used by the Django admin delete action) now deletes and uploads correctly with S3 mocked. Deliberately did not touch `scuba/galleries/models.py`'s own additional stacked bugs (wrong buffer type, reused text buffer, float-vs-int coordinates) — those are cited separately in `CODE_REVIEW.md` §4.5 as part of the broader "galleries is non-functional end-to-end" finding, already called out in this file's Known Technical Debt, and out of scope for the shared cross-app pipeline fix.
- 2026-08-06: Closed `CODE_REVIEW.md` §2.4 (weak/unenforced password policy). `scuba/accounts/validators/signup.py`'s `validate_password` (used by the live `SignupForm`) now fixes the operator-precedence bug and actually invokes Django's configured `AUTH_PASSWORD_VALIDATORS` (`MinimumLengthValidator`, `CommonPasswordValidator`, `NumericPasswordValidator`, `UserAttributeSimilarityValidator`) via `django.contrib.auth.password_validation.validate_password`, on top of this project's existing 20-character maximum; it now raises `django.core.exceptions.ValidationError` with the real reason(s) instead of returning a bare bool, and `SignupForm.clean_password()`/the new `RegisterSerializer.validate_password()` both surface those real messages. Fixed the same precedence bug in the duplicate, currently-dead `validate_password` in `scuba/accounts/forms/__init__.py` (confirmed unreferenced anywhere else in the codebase, so this is hygiene, not a behavior change). `RegisterSerializer` (`scuba/accounts/serializers/account.py`, live at `POST /register`) had no password validation at all before this change. `SetPasswordApi`/`SetUsernameApi` (`scuba/accounts/apis/signup.py`) were silent no-ops — `serializer.is_valid()` was called but `.save()` never was; both now pass `instance=request.user` and call `.save()`. Fixing the no-op surfaced two more live bugs in the same code that would have broken the moment persistence started working: `SetUsernameSerializer.update()`/`SetPasswordSerializer.update()` read `self.context['request'].user`, but the views only ever passed `context={'user': request.user}` (wrong key), and both `update()` methods incorrectly indexed `instance` (`instance['username']`) as if it were the validated-data dict rather than the model instance DRF actually passes — both now correctly use `validated_data['...']` against the real `instance` parameter and call `instance.save()` (the password variant was also missing `.save()` entirely, so `set_password()` alone would never have persisted). Also found and fixed: `SetUsernameApi` was `permission_classes = (AllowAny,)`, letting anonymous callers hit it (now falls back to the project's `IsAuthenticated` default, matching `SetPasswordApi`); and `SetUsernameSerializer.validate_username` didn't exclude the caller's own current username from its uniqueness check, which would have made re-submitting an unchanged username fail as "already registered" the moment the no-op bug was fixed — now excludes `self.instance.pk`.
- 2026-08-06: Closed `CODE_REVIEW.md` §2.3 (unauthenticated `/tmp`-writing webhooks + stored XSS chain). All three SNS-driven webhooks — `CodeBuildAPI`/`CodePipelineAPI` (`scuba/aws/apis.py`, mounted at `/aws/cicd/build`/`/aws/cicd/pipeline`) and `BouncedEmailsAPI` (`scuba/security/apis.py`, mounted at `/security/emails/bounced`) — now call a new `scuba.libs.aws.sns.verify_signature()` at the top of `post()` and reject (403) any message that doesn't carry a valid AWS SNS signature, closing the actual "unauthenticated" hole rather than just tidying around it. `verify_signature()` (`scuba/libs/aws/sns.py`) restricts `SigningCertURL` to real `sns.<region>.amazonaws.com` hosts over https (blocks SSRF/self-signed-cert spoofing), fetches the cert, and verifies the RSA signature over AWS's documented canonical string per https://docs.aws.amazon.com/sns/latest/dg/sns-verify-signature-of-message.html. Added `cryptography==49.0.0` to `requirements.txt` as a direct dependency (was already present transitively). Also fixed the three smaller bugs cited alongside this finding: removed the unbounded, unauthenticated `/tmp/build.txt`/`/tmp/pipeline.txt`/`/tmp/bounced.txt` disk writes (replaced with normal `logging` calls, gated behind the new signature check so only verified payloads are logged at all); fixed the stored-XSS chain in `scuba/aws/admin.py`'s `reports()` (swapped `mark_safe(f'...{obj.id}...')` for `format_html(...)`, which auto-escapes `obj.id`); fixed the literal `status.HTTP_400_HTTP_400_BAD_REQUEST` typo (was `AttributeError`-crashing the "malformed payload" branch, now a real `HTTP_400_BAD_REQUEST`) and added the missing `try/except KeyError` around `CodePipelineAPI.post`'s message parsing, matching `CodeBuildAPI`; removed unreachable duplicate dead code after the first `return` in `CodeBuildAPI.post`; added the missing `@staticmethod` on `SNSSubscriptionRequestSerializer.validate_signature`/`validate_message_id`/`validate_timestamp` (`scuba/sitesettings/serializers.py`) as cited, though see the new Known Technical Debt entry below — this fix turned out to be inert for a separate, previously-undocumented reason discovered while testing.
- 2026-08-06: Closed `CODE_REVIEW.md` §2.2 (unauthenticated admin actions). `scuba/accounts/admin.py`'s custom `.../reset-password/` and `.../emails/welcome/` URLs are now wrapped in `self.admin_site.admin_view(...)`, same as `.../impersonate/` — both now require a logged-in staff user, closing the anonymous probe-and-spam path. Also fixed the two "broken even when reached" bugs cited alongside the auth gap: `reset_password` now checks `form.is_valid()` before calling `.save()` (previously called `.save()` unconditionally); `send_welcome_email_to_user` now uses `get_object_or_404` (404 instead of an unhandled `DoesNotExist` 500 on a bad id) and catches the `TypeError` from calling `user.send_welcome_email()` with no arguments, showing an admin-facing error instead of a raw 500. That underlying `TypeError` is not actually fixable within this task's scope: `send_welcome_email(self, email_template)` requires an `EmailTemplate` instance, but no `EmailTemplate` model exists anywhere in the current codebase (confirmed via `grep` and `git log -S`) — it's referenced once, in `send_confirmation_code_email`, and was apparently removed in an earlier commit. Rebuilding that model was out of scope (would violate "never invent models") and is left as a separate, undocumented-until-now gap — see Known Technical Debt.
- 2026-08-05: Replaced the admin-impersonation backdoor with an audited "login as user" support tool. Removed `scuba/libs/authentication/adminoverride.py` and its `AUTHENTICATION_BACKENDS` entry entirely (it let anyone who knew any admin's real email+password log in as any other user via the standard login path, with no audit trail). Replaced it with `scuba.security.services.impersonation` (`start_impersonation`/`stop_impersonation`), a new audited `security.ImpersonationEvent` model (actor, target, reason, started_at/ended_at), and a Django Admin-integrated trigger: a "Impersonate" action/link on the User admin that requires a typed reason and is gated on the real `is_superuser` flag (not `is_admin`/`is_staff`). Superusers cannot impersonate other superusers or themselves. A site-wide banner (via the `Scuba` context processor + `templates/layout.html`) shows "viewing as a support session" with a stop button while impersonating; `POST /impersonate/stop/` restores the original admin's session and closes the audit event. This is the app's first-ever migration for the `security` app (`scuba/security/migrations/0001_initial.py`) since no app in the project had migrations before now — it also captures the pre-existing `BlockedCountry`/`InvalidEmail`/`BouncedEmail`/`InvalidSignup` models as part of that same initial migration, which is expected for a first migration, not scope creep. Verified locally against an empty `db.sqlite3`; if a real deployed database already has these tables, `migrate --fake-initial` will be needed there instead of a plain `migrate`.
- 2026-08-05: Redid the full manual code review on `main` (previously only existed on the `restart` branch, which `main` predates and does not include). Reviewed `scuba/accounts` directly; delegated the "dive domain" cluster (divesites, diveshops, equipment, logbooks, galleries, divegroups, maps, entities, search) and "infra/platform" cluster (sitesettings, content, home, security, aws, libs, robots, environ, system, cache, settings.py) to two independent review passes. Result written to `CODE_REVIEW.md` on `main`. See that file for the full findings; highlights below under Known Technical Debt.
- 2026-08-05: Added `django-ses==4.7.2` to `requirements.txt` on `main` (it was already configured as `EMAIL_BACKEND` but missing from pinned dependencies, so any environment built strictly from `requirements.txt` would hit `ModuleNotFoundError` on the first outgoing email).

## Known Technical Debt

- [ ] Complete CI baseline.
- [ ] Verify all tests avoid live external calls.
- [ ] Finish environment-based secret configuration.
- [ ] Validate current migrations against MySQL.
- [ ] Continue model cleanup using data-preserving migrations.
- [ ] `manage.py makemigrations --check --dry-run` run with no app_label silently reports "No changes detected" even when unmigrated apps genuinely need an initial migration (confirmed 2026-08-05: `security` had zero migrations, `makemigrations --check --dry-run` said clean, but `makemigrations security` correctly generated `0001_initial.py`). Root cause not fully isolated — appears tied to how `MigrationAutodetector.changes()` is invoked across many simultaneously-unmigrated apps. Until every app has at least one migration, validate migration drift per-app (`makemigrations <app> --check --dry-run`), not project-wide.
- [ ] `scuba/accounts/tests/test_api_profile.py` (`test_me_profile`, `test_user_profile_1`) and `scuba/accounts/tests/test_api_user_profiles.py` (`test_get_basic_profile_is_not_private`, `test_get_basic_profile_is_private`, `test_get_basic_profile_is_private_but_buddies`) fail on a truly fresh test database with `no such table: view_profile` — confirmed on unmodified `main` via a throwaway worktree, so this is pre-existing, not a regression from any single session's changes. `accounts.models.ViewProfile` (`db_table='view_profile', managed=False`) expects a DB view that nothing in the codebase creates (no migrations, no `CREATE VIEW` anywhere). A prior session's "129 passed, 1 failed" pytest baseline did not reflect this; treat that number as stale. Needs either a data migration that creates the view or a rework of `apis/profile.py` to stop depending on it. As of 2026-08-06 this is the *only* known-failing test in the suite (`test_blocked_ip_address` is gone, see Recently Completed).
- [ ] `BlockedCountry`/`InvalidSignup` (`scuba/security/models.py`) are now fully orphaned — nothing in the live app references them anymore after the §2.5 cleanup (2026-08-06), only their admin registrations and migration remain. Deliberately not deleted (would be a schema change with data-loss potential); revisit if the project decides it never wants IP/country-based signup blocking at all.
- [x] ~~Fix `scuba/libs/authentication/adminoverride.py`~~ — done 2026-08-05, see Recently Completed.
- [x] ~~Wrap `scuba/accounts/admin.py`'s custom `reset-password`/`emails/welcome` URLs in `admin_site.admin_view(...)`~~ — done 2026-08-06, see Recently Completed.
- [ ] No `EmailTemplate` model exists anywhere in the codebase, despite `scuba/accounts/models.py`'s `send_welcome_email()`/`send_confirmation_code_email()` both requiring one — confirmed via `git log -S"class EmailTemplate"` that it existed historically and was removed. Until it (or a replacement) is rebuilt, both the admin "Send Welcome Email" action and any confirmation-code email path are non-functional. Decide whether to rebuild the model, or rework these methods to not depend on a DB-backed template.
- [x] ~~Remove/rework the unauthenticated `/tmp`-writing webhooks in `scuba/aws/apis.py` and `scuba/security/apis.py`, and fix the stored-XSS chain in `scuba/aws/admin.py`'s `reports()`~~ — done 2026-08-06, see Recently Completed.
- [ ] `SNSSubscriptionRequestSerializer.validate_signature`/`validate_message_id`/`validate_timestamp` (`scuba/sitesettings/serializers.py`) are named to look like DRF per-field validation hooks but never actually run: DRF calls `validate_<FieldName>` using the exact declared field name (e.g. `validate_MessageId`, `validate_Timestamp`), not a lowercased/underscored version, so these three are silently dead code today — only `validate_SigningCertURL` (whose name happens to match exactly) ever executes. This is a separate bug from the now-fixed missing-`@staticmethod` issue (`CODE_REVIEW.md` §2.3) and was not previously documented. Do not simply rename them to fix this: `SNSSubscriptionRequestSerializer.create()` (`serializers.py:72-76`) calls `datetime.strptime(validated_data['timestamp'], ...)` expecting a raw string — if `validate_Timestamp` actually ran and returned a parsed `datetime` (as `validate_timestamp` currently does, unreachably), `create()` would break on that already-parsed value. Fixing this needs `create()` reworked at the same time, not just the hook renamed. Low priority: this whole serializer lives in `sitesettings`, which the roadmap already plans to retire (`MODERNIZATION_ROADMAP.md` "Retire `scuba.sitesettings`").
- [x] ~~Actually enforce a password policy: `RegisterSerializer` (accounts) has none, and `validate_password` has a 4-20 char range plus an operator-precedence bug~~ — done 2026-08-06, see Recently Completed.
- [x] ~~`SetPasswordApi`/`SetUsernameApi` validate but never call `.save()`/`.update()` — both endpoints are silent no-ops~~ — done 2026-08-06, see Recently Completed.
- [x] ~~Reconnect or remove the dead IP/country signup-blocking code in `scuba/accounts/forms/signup.py`~~ — removed 2026-08-06, see Recently Completed.
- [x] ~~Add authorization to every view in `scuba/equipment/views.py` — currently no login/ownership checks at all~~ — done 2026-08-06, see Recently Completed.
- [x] ~~Fix the image/S3 upload pipeline — broken in 5+ independent places across `scuba/libs/imageuploader.py`, `scuba/libs/aws/s3.py`, `scuba/libs/models/awsmodel.py`~~ — done 2026-08-06, see Recently Completed (the `scuba/galleries/models.py`-specific stacked bugs from §4.5 remain, tracked below).
- [ ] `galleries`, `logbooks`, `diveshops`, `divegroups` are non-functional end-to-end (undefined methods/fields, dead code, broken imports) — see `CODE_REVIEW.md` §4 for the full per-app list before scoping any new feature work in these apps.
- [ ] `scuba/accounts/forms/__init__.py`'s `SettingsForm`/`PasswordForm` bind to `django.contrib.auth.models.User` instead of the project's swapped `AUTH_USER_MODEL`, and are wired live into `/settings/account`/`/settings/password/` — will fail the moment a user tries to use them (`CODE_REVIEW.md` §3.1).
- [ ] Add DRF throttling and cookie/TLS hardening settings (`SESSION_COOKIE_SECURE`, `CSRF_COOKIE_SECURE`, etc.) — currently absent project-wide (`CODE_REVIEW.md` §5).
- [ ] Add `timeout=` to every outbound `requests.get`/`requests.post` call (weather, chat, logbook, settings, alerting servers) — currently unbounded everywhere they were checked (`CODE_REVIEW.md` §2, §4, §5, multiple entries).

## Decision Log

Record durable decisions with a date.

### Template

```text
YYYY-MM-DD: Decision title

Decision:
...

Reason:
...

Consequences:
...
```

### 2026-08-05: Redo the full code review against `main` instead of relying on the `restart`-branch review

Decision:
Ran a fresh, independent full-codebase review on `main` rather than assuming the earlier review (done on `restart`, which branched later and applied fixes `main` doesn't have) still applied.

Reason:
`main` and `restart` had diverged — `restart` already fixed `USE_TZ`, the SES backend, and some settings.py issues that are still present, unfixed, on `main`. Reusing the old review's findings without re-verifying against `main`'s actual code would have risked stale/incorrect claims.

Consequences:
`CODE_REVIEW.md` on `main` is now a from-scratch, independently-verified document (not a copy of the `restart`-branch one) and found a substantially larger set of issues than the original pass, including several not previously documented (e.g. the `galleries`/`logbooks`/`diveshops` apps being non-functional end-to-end, the stored-XSS chain in `scuba/aws/admin.py`, and the non-functional `SetPasswordApi`/`SetUsernameApi` endpoints). Before merging or comparing the two branches, treat `main`'s `CODE_REVIEW.md` as authoritative for `main`'s actual state.

## Handoff Notes

At the end of a substantial session, record:

- current branch;
- task status;
- exact files changed;
- commands run;
- failing tests;
- unresolved design questions;
- next recommended step.

### 2026-08-05 session handoff

- Branch: `main`.
- Task status: full code review redone and written to `CODE_REVIEW.md`; `django-ses` dependency gap fixed; no other code changes made (findings-only pass, matching the original review's scope).
- Files changed: `CODE_REVIEW.md` (new content), `requirements.txt` (added `django-ses==4.7.2`), `docs/claude/TASK_TRACKER.md` (this update).
- Commands run: `python manage.py check` (clean), `python manage.py makemigrations --check --dry-run` (no changes detected — no migrations exist for any custom app), `pytest -q` (129 passed, 1 failed — `test_blocked_ip_address`, 5 skipped), `python -c "from django_ses import SESBackend"` (succeeds after the requirements.txt fix).
- Failing tests: `scuba/accounts/tests/test_account_forms_signup.py::TestAccountFormSignup::test_blocked_ip_address` — pre-existing, root-caused in `CODE_REVIEW.md` §2.5 (dead IP/country block-check code in `SignupForm`).
- Unresolved design questions: none raised this session; next step is prioritizing which CRITICAL findings in `CODE_REVIEW.md` to fix first (the admin-impersonation backdoor and the unauthenticated `/tmp`-writing webhooks are the highest-blast-radius items).
- Next recommended step: pick one CRITICAL item from `CODE_REVIEW.md` §2 to fix (with tests) rather than attempting the whole list at once, per the project's "smallest coherent change" rule.

### 2026-08-05 session handoff (impersonation backdoor fix)

- Branch: `main`.
- Task status: complete. Replaced the `AdminOverride` impersonation backdoor with an audited Django-Admin-integrated "login as user" tool, gated on real `is_superuser`. See Recently Completed above for full behavior description.
- Files created: `scuba/security/services/__init__.py`, `scuba/security/services/impersonation.py`, `scuba/security/migrations/__init__.py`, `scuba/security/migrations/0001_initial.py`, `scuba/accounts/views/impersonation.py`, `scuba/accounts/templates/accounts/admin/impersonate_confirm.html`, `scuba/accounts/tests/test_admin_impersonation.py`, `scuba/security/tests/test_impersonation_service.py`.
- Files modified: `scuba/security/models.py` (new `ImpersonationEvent` model), `scuba/security/admin.py` (read-only admin registration for it), `scuba/accounts/admin.py` (impersonate action/view/URL on `UserAdmin`), `scuba/accounts/templates/accounts/admin/change_user_form.html` (object-tools link), `scuba/libs/context_processors/scuba.py` (`impersonating` flag), `templates/layout.html` (banner + stop button), `scuba/settings.py` (removed `AdminOverride` from `AUTHENTICATION_BACKENDS`), `scuba/urls.py` (`/impersonate/stop/`), `docs/claude/SECURITY.md`, `docs/claude/TASK_TRACKER.md`.
- Files deleted: `scuba/libs/authentication/adminoverride.py`, `scuba/libs/tests/test_authentication_adminoverride.py`.
- Migrations: `scuba/security/migrations/0001_initial.py` — first-ever migration for the `security` app (it had none before). Bundles `ImpersonationEvent` together with the app's pre-existing `BlockedCountry`/`InvalidEmail`/`BouncedEmail`/`InvalidSignup` models, since Django has to snapshot the whole app in its first migration. Verified against a fresh, empty `db.sqlite3` (`migrate` succeeds cleanly). Not tested against any database that might already have these tables from another source (e.g. a deployed environment built before migrations existed) — that would need `migrate --fake-initial` instead of a plain `migrate`, and was out of scope to verify without access to such an environment.
- Tests added: 13 new (`test_admin_impersonation.py` x8, `test_impersonation_service.py` x5), all passing.
- Commands run (venv: `/Coding/scubamob/ENV`): `manage.py check` (clean, only the pre-existing `staticfiles.W004` warning), `manage.py makemigrations security` (generated `0001_initial.py`), `manage.py makemigrations --check --dry-run` (no changes detected, expected now that `security` has migrations), `pytest` full suite (130 passed, 6 failed, 5 skipped).
- Failing tests (all pre-existing, none caused by this change — verified by running the same tests against unmodified `main` in a throwaway `git worktree`): `test_blocked_ip_address` (already known, `CODE_REVIEW.md` §2.5) plus 5 `view_profile`-table-missing failures in `test_api_profile.py`/`test_api_user_profiles.py` that were not previously documented — see the new Known Technical Debt entry above. The prior session's "129 passed, 1 failed" figure did not reflect these 5; treat it as stale.
- Also discovered and documented as technical debt: project-wide `makemigrations --check --dry-run` silently reports "No changes detected" even when an unmigrated app genuinely needs its first migration — only per-app invocation (`makemigrations <app> --check --dry-run`) is reliable until every app has at least one migration.
- Unresolved design questions: none — user chose Django-Admin-integrated trigger over a new API endpoint, and chose to block impersonating other superusers, both up front.
- Next recommended step: pick the next CRITICAL item from `CODE_REVIEW.md` §2 (unauthenticated `/tmp`-writing webhooks or the broken image/S3 upload pipeline are the next-highest blast radius).

### 2026-08-06 session handoff (unauthenticated admin actions fix)

- Branch: `main`.
- Task status: complete. Closed `CODE_REVIEW.md` §2.2 — see Recently Completed above for full behavior description.
- Files created: `scuba/accounts/tests/test_admin_user_actions.py`.
- Files modified: `scuba/accounts/admin.py` (`get_urls` wraps `reset_password`/`send_welcome_email_to_user` in `admin_site.admin_view(...)`; `reset_password` checks `form.is_valid()`; `send_welcome_email_to_user` uses `get_object_or_404` and catches the `TypeError`), `docs/claude/TASK_TRACKER.md` (this update).
- Migrations: none (no model changes).
- Tests added: 6 (`test_admin_user_actions.py`) — anonymous/non-staff redirected to login on both URLs, staff can trigger a password-reset email, staff hitting the welcome-email URL gets a clean redirect + error message instead of a 500. All passing.
- Commands run (venv: `/Coding/scubamob/ENV`): `manage.py check` (clean), `manage.py makemigrations accounts --check --dry-run` (reports a full initial migration needed — pre-existing, `accounts` has never had migrations, unrelated to this change), `pytest scuba/accounts/tests/test_admin_user_actions.py` (6 passed), `pytest` full suite (136 passed, 6 failed, 5 skipped — same 6 pre-existing failures as the prior session, no new failures).
- Failing tests: unchanged from the prior session (`test_blocked_ip_address` plus the 5 `view_profile`-table-missing tests) — none caused by this change.
- Unresolved design questions: none. Discovered along the way that `EmailTemplate` (required by `send_welcome_email`/`send_confirmation_code_email`) doesn't exist anywhere in the codebase — flagged as new Known Technical Debt rather than invented or silently worked around.
- Next recommended step: pick the next CRITICAL item from `CODE_REVIEW.md` §2 — the unauthenticated `/tmp`-writing webhooks + stored-XSS chain (`scuba/aws/apis.py`, `scuba/security/apis.py`, `scuba/aws/admin.py`) are the next-highest blast radius.

### 2026-08-06 session handoff (unauthenticated webhooks + stored XSS fix)

- Branch: `main`.
- Task status: complete. Closed `CODE_REVIEW.md` §2.3 — see Recently Completed above for full behavior description.
- Files created: `scuba/libs/aws/sns.py` (SNS signature verification service), `scuba/libs/aws/testing.py` (test-only helper: real self-signed cert + real signature generation, reused across three test files rather than duplicated), `scuba/libs/tests/test_aws_sns.py`, `scuba/aws/tests/test_api_webhooks.py`.
- Files modified: `scuba/aws/apis.py` (both views call `verify_signature()` first, `/tmp` writes replaced with `logging`, `HTTP_400_HTTP_400_BAD_REQUEST` typo fixed, `CodePipelineAPI.post` wrapped in `try/except KeyError` to match `CodeBuildAPI`, dead duplicate code removed), `scuba/security/apis.py` (same signature-verification gate, `/tmp` write replaced with `logging`), `scuba/aws/admin.py` (`reports()` now uses `format_html` instead of `mark_safe(f'...')`, closing the stored XSS), `scuba/sitesettings/serializers.py` (`@staticmethod` added to the three cited validators), `scuba/security/tests/test_blocked_emails.py` (rewritten — the old fixture used a fake `"Signature": "SECRET"` that the previously-broken verification silently accepted; it now must be a real signature), `requirements.txt` (`cryptography==49.0.0`, was already present transitively, now a direct dependency), `docs/claude/TASK_TRACKER.md` (this update).
- Migrations: none (no model changes).
- Tests added: 16 (`test_aws_sns.py` x7 unit tests on the verification logic itself: valid signature, tampered message, wrong keypair, untrusted host, plain-http cert URL, missing signature, missing required field; `test_api_webhooks.py` x6 integration tests across both AWS endpoints: untrusted host / forged signature / valid signature × 2 endpoints; `test_blocked_emails.py` x3, replacing the 2 old ones). All passing, no live network calls (cert fetch is mocked via `requests.get` patching in every test that reaches it; the two host-validation tests reject before any network call would happen, so they don't even need mocking).
- Commands run (venv: `/Coding/scubamob/ENV`): `manage.py check` (clean), `manage.py makemigrations aws security sitesettings --check --dry-run` (`aws` and `sitesettings` report full initial migrations needed — pre-existing, neither app has ever had migrations, unrelated to this change; `security` reports clean, as expected since it already has migrations from the prior session), `pytest` full suite (150 passed, 6 failed, 5 skipped — same 6 pre-existing failures as every prior session this week, no new failures).
- Failing tests: unchanged from prior sessions (`test_blocked_ip_address` plus the 5 `view_profile`-table-missing tests) — none caused by this change.
- Unresolved design questions: none explicitly asked, but one scoping judgment call worth flagging — I implemented real cryptographic SNS signature verification (not just removing the `/tmp` writes and escaping the XSS) because that's what actually closes "unauthenticated" per the finding's own title; a narrower fix would have left the endpoints just as spoofable, only without the disk-exhaustion and XSS side effects. Also discovered along the way (documented as new Known Technical Debt above, not fixed): `SNSSubscriptionRequestSerializer.validate_signature`/`validate_message_id`/`validate_timestamp` are dead code today due to a DRF hook-naming mismatch, independent of the `@staticmethod` bug I did fix.
- Next recommended step: pick the next CRITICAL item from `CODE_REVIEW.md` §2 — weak/unenforced password policy (`RegisterSerializer` has none, `validate_password` has an operator-precedence bug) and the no-op `SetPasswordApi`/`SetUsernameApi` endpoints are the next-highest priority; the broken image/S3 upload pipeline (5+ independent bugs) is the other large remaining CRITICAL item.

### 2026-08-06 session handoff (password policy + image/S3 upload pipeline fix)

- Branch: `main`.
- Task status: complete. Closed both `CODE_REVIEW.md` §2.4 and §2.7 in one session (user asked for both together) — see Recently Completed above for full behavior description of each.
- Files created: `scuba/libs/tests/test_imageuploader.py`, `scuba/libs/tests/test_awsmodel.py`, `scuba/libs/tests/test_fileutils.py`.
- Files modified (§2.4): `scuba/accounts/validators/signup.py`, `scuba/accounts/forms/signup.py`, `scuba/accounts/forms/__init__.py`, `scuba/accounts/serializers/account.py`, `scuba/accounts/apis/signup.py`, `scuba/accounts/serializers/signup.py`, plus tests: `scuba/accounts/tests/test_validators_signup.py`, `scuba/accounts/tests/test_account_forms_signup.py`, `scuba/accounts/tests/test_api_user_register.py`, `scuba/accounts/tests/test_api_setpassword.py`, `scuba/accounts/tests/test_api_createuser.py`.
- Files modified (§2.7): `scuba/libs/imageuploader.py`, `scuba/libs/models/awsmodel.py`, `scuba/libs/fileutils.py`.
- Files modified (both): `docs/claude/TASK_TRACKER.md` (this update).
- Migrations: none (no model changes).
- Tests added/changed: §2.4 — 3 new tests (`test_user_register_weak_password_is_rejected`, `test_set_own_current_username_is_not_rejected_as_duplicate`, `test_set_username_requires_authentication`) plus 2 rewritten (`test_validator_password` now asserts raises instead of bool return; `test_bad_password_length` now asserts the real Django validator messages) and 2 existing tests updated to use passwords/auth that match the new, real enforcement (`test_user_register_good`'s password was literally `'password'`, now common-password-rejected so changed to a strong one; `test_set_duplicate_username` now authenticates, since the endpoint correctly requires it now) plus 2 new persistence assertions added to previously-passing "good" tests (`test_set_good_password`, `test_set_username`) to verify the no-op bug is actually fixed, not just that the endpoint returns 200. §2.7 — 6 new tests across the three new files, S3 fully mocked in all of them.
- Commands run (venv: `/Coding/scubamob/ENV`): `manage.py check` (clean), `pytest` full suite (159 passed, 6 failed, 5 skipped — same 6 pre-existing failures as every prior session this week, no new failures).
- Failing tests: unchanged from prior sessions (`test_blocked_ip_address` plus the 5 `view_profile`-table-missing tests) — none caused by this change.
- Unresolved design questions: none explicitly asked. Scoping judgment calls made: (1) for §2.4, routed the live `validate_password` through Django's real `AUTH_PASSWORD_VALIDATORS` rather than just fixing the precedence bug, since the finding's own text says the point is that those validators are "effectively dead configuration" — a narrower fix would have left them dead. This required updating several test fixtures that used trivially weak passwords (`'password'`, `'test1234'`, `'xxxxxxxx'`) as their "known good" cases. (2) For §2.7, fixed only the shared cross-app pipeline (`imageuploader.py`, `awsmodel.py`, `fileutils.py`) and deliberately left `scuba/galleries/models.py`'s own additional stacked bugs (§4.5) untouched — that's a separate, already-tracked, larger per-app finding, not part of the CRITICAL cross-app item.
- Next recommended step: §2's CRITICAL items are now all closed. Remaining CODE_REVIEW.md work is HIGH/MEDIUM: §2.5 (dead IP/country signup-blocking code — also the root cause of the long-standing `test_blocked_ip_address` failure), §2.6 (no authorization anywhere in `scuba/equipment/views.py`), or start on the per-app clusters in §3 (accounts deep-dive) / §4 (dive domain: galleries/logbooks/diveshops/divegroups are non-functional end-to-end).

### 2026-08-06 session handoff (§2.5 dead code removal + §2.6 equipment authorization)

- Branch: `main`.
- Task status: complete. Closed both `CODE_REVIEW.md` §2.5 (removed, not reconnected, per explicit instruction) and §2.6 — see Recently Completed above for full behavior description of each. All of §2 is now closed.
- Files created: `scuba/equipment/tests/test_views_authorization.py`.
- Files modified (§2.5): `scuba/accounts/forms/signup.py` (removed `__init__`/`set_ip_address`, the unused `BlockedCountry`/`InvalidSignup`/`InvalidIPAddress` imports, and the dead `iso_country`/`add_signup_country` branch in `save()`), `scuba/accounts/views/signup.py` (removed `SignupView.get_form_kwargs()` entirely and the `form.set_ip_address(...)` call in `ValidateEmail.post()`), `scuba/accounts/tests/test_account_forms_signup.py` (dropped `ip_address=` from all 6 remaining `SignupForm(...)` calls, deleted `test_blocked_ip_address` and its now-unused `BlockedCountry` import).
- Files modified (§2.6): `scuba/equipment/views.py` — every view now `@login_required` and ownership-scoped; also fixed the pre-existing, unrelated `Context()`-API and wrong-template-path bugs that were making every view except `edit`/`delete_equipment` 500 unconditionally (see Recently Completed for detail on why these were in-scope to fix alongside the auth work).
- Files modified (both): `docs/claude/TASK_TRACKER.md` (this update).
- Migrations: none (no model changes).
- Tests added/changed: §2.5 — no new tests (the feature was deleted, not reworked); 1 test file trimmed. §2.6 — 11 new tests in `test_views_authorization.py`, covering: anonymous access redirected to login on all 9 URLs; `index`/`archive2`/`archive3` only show the caller's own records; edit/delete blocked (404) on another user's `Equipment` and `EquipmentMaintenance`; own-record edit/delete succeeds; a new maintenance requirement cannot be attached to another user's equipment via the form's `equipment` field, but can be attached to the caller's own. One test bug caught and fixed during this session: my first version of the "cannot attach to another user's equipment" test asserted against a filter that collided with a `setUp()` fixture row and would have passed even if the underlying authorization were broken — rewrote it to use a distinguishing value instead of trusting the first (wrongly-passing) result.
- Commands run (venv: `/Coding/scubamob/ENV`): `manage.py check` (clean), `pytest scuba/accounts/tests/test_account_forms_signup.py` (6 passed), `pytest scuba/equipment/tests/test_views_authorization.py` (11 passed), `pytest` full suite (170 passed, 5 failed, 5 skipped).
- Failing tests: only the 5 pre-existing `view_profile`-table-missing tests remain. `test_blocked_ip_address` — the project's single longest-standing known-failing test, flagged in every session this week — no longer exists (feature and test both removed), so the suite has no other known failures.
- Unresolved design questions: none explicitly asked. Scoping judgment calls made: (1) for §2.5, left the `BlockedCountry`/`InvalidSignup` models, admin registrations, and migration in place rather than deleting them — removing DB-backed models is a schema change with more blast radius than what was asked, and risks data loss in a real deployment; flagged as new Known Technical Debt instead so the decision is visible and explicit rather than silently made. (2) For §2.6, fixed two unrelated pre-existing bugs (old-style `Context()` API, wrong template paths) that were in the exact lines being rewritten and would have left the authorization fix functionally unverifiable (every affected view 500ing regardless of who's calling) — same judgment-call pattern used in prior sessions this week (e.g. the SNS serializer context-key fix, the imageuploader Pillow/S3 argument-order fix).
- Next recommended step: `CODE_REVIEW.md` §2 (all cross-app CRITICAL/HIGH findings) is now fully closed. Next candidates: §3 (`scuba/accounts` deep-dive findings not already covered above) or §4 (the dive-domain cluster — `galleries`, `logbooks`, `diveshops`, `divegroups` are non-functional end-to-end; read the full per-app breakdown in `CODE_REVIEW.md` before scoping any single fix there, since several of these apps have many stacked bugs each). §5 (DRF throttling, cookie/TLS hardening settings) is a smaller, more contained next step if a quick win is preferred over the larger per-app clusters.
